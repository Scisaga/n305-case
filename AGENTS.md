# N305 项目工作规则

本文件适用于整个仓库。目标是从照片和用户实测数据逐步建立可审阅、可追溯、可生成 CAD 的 N305 主板与外壳模型。

## 1. 唯一坐标系

- 正对风扇面且鳍片在下：上方 06、下方 04、左边 07、右边 05。
- CAD `+X` 指向 06，`-X` 指向 04；`+Y` 指向 07，`-Y` 指向 05；`+Z` 指向风扇面。
- PCB 为 `100.0 × 105.5 × 1.5 mm`；PCB 底面为 `Z=0`，顶面为 `Z=1.5`。
- 04/06 面板正投影以 PCB 边中心为 `Y=0`，不得使用照片画面边缘作为机械原点。

## 2. 数据权威顺序

按以下顺序解决冲突，不得反过来：

1. 用户明确给出的实测尺寸和方向；
2. 卡尺/标尺照片中的可读实测数据；
3. 已完成 PCB 基准对齐的正投影标定；
4. 原壳照片中的轮廓、接口顺序和结构语义；
5. 视觉推断。

第 5 类必须标注为 provisional，不得伪装成实测值。当前面板机械数据的唯一源是 `src/n305_panel_reference.py`；机器可读副本是 `docs/panel-apertures.json`。照片像素记录只保存在 `src/n305_photo_reference.py` 和 `docs/photo-calibration.json`。

## 3. 禁止混淆照片与机械图

- 原壳照片只用于确认孔的类型、顺序和非标准轮廓；照片透视、斜边和像素间距不得直接进入 STL/STEP。
- 局部 1 mm 照片网格只验证单孔尺寸，不是共享坐标系，不能推导孔间距或 PCB 坐标。
- CAD 参考图必须是正投影、统一毫米比例，并显示 PCB 边界、坐标方向、孔中心、标称尺寸和数据来源。
- 两个 06 USB 孔必须是水平 `12.8 × 5.5 mm`、R0.7 圆角矩形；中心 Z 可不同，但孔本身不得倾斜。
- HDMI 使用原壳六边形，不使用通用“标准 HDMI 梯形”。
- 主板参考中的通用 HDMI 鼻端占位不得用于外壳开孔覆盖；真实前端轮廓未测量时应明确标为“不计算”，不得用代理产生假冲突或反向修改六边形孔。
- RJ45 的 `15 × 10 mm` 是完整主窗；`4.5 × 1 mm` relief 额外位于主窗下方。
- 后续 CAD 必须直接导入毫米参数，禁止从参考 PNG 反算尺寸。

## 4. 固定工作流与审阅门

严格按以下顺序工作：

1. 保存原始照片，不在原图上写入不可逆修改。
2. 建立照片像素证据，明确哪些值是实测、标定或推断。
3. 把数据转换到统一 PCB 坐标，生成带 PCB 边界的正投影参考图和机器可读参数。
4. 由用户审阅尺寸、方向、中心和轮廓。
5. 审阅通过后才生成对应 STEP/STL。
6. 对三维输出执行尺寸、边界、接触、开孔覆盖和文件清单验证。

若用户明确要求某一已审阅对象进入三维阶段，可以只对该对象执行第 5～6 步；不得顺便生成尚未审阅的外壳结构。

## 5. 三维参考模型要求

- 物理实体、面板开孔、插头通道和保守避让体必须分开命名和导出。
- 主板四个安装孔必须来自机器参数，不得按对称关系重新猜测。
- 01/02 卡尺图确认的是 PCB 平面 `100.0 × 105.5 mm`，不是接口最大包络；禁止再把 `103.4-100.0` 平分成两侧各 `1.7 mm`。
- X 向突出量必须按实体分别保存：04 双 USB 最大突出暂按用户粗测 `2.0 mm`，06 两个 USB 按 `1.0 mm`，04 鳍片外沿与 PCB 的 `X=-50.0 mm` 边齐平。04 其余接口的“略凸”代理必须标为 provisional，不得伪装成逐件实测。
- 风扇平面外形必须读取 01 照片经 PCB 校正后的非对称蜗壳轮廓；不得再用圆角矩形代替。叶轮应表达为多叶片离心叶轮，不得使用三根径向杆代替。
- 顶视照片中的遮挡关系只能确定 XY，不能确定 Z。01 中的双孔长条不得放在风扇上表面；在缺少侧视安装证据时直接省略，若以后恢复则必须先证明它与 PCB 的高度和连接关系。
- 散热组件必须存在清楚的接触路径：`PCB → 冷板/支撑 → 风扇托板 → 热桥 → 鳍片`；禁止悬空风扇或悬空鳍片。
- 验证文件必须检查 PCB 包络、裸组件总厚度、四孔位置、散热接触和各输出实体边界。
- STEP/STL 输出放入 `exports/reference/` 的明确子目录；预览放入 `previews/reference/`；不要在仓库根目录散落生成物。
- 不得生成外壳 STEP/STL，除非用户明确批准外壳三维阶段。
- 当前外壳结构审阅的唯一参数源是 `src/n305_enclosure_structure.py`；机器副本是 `docs/enclosure-structure.json`。结构审图脚本只可写入 `previews/enclosure/` 和 `docs/`，在用户确认本阶段以前不得向 `exports/` 写入外壳实体。
- 04、06 是各自完整的连续平面侧壁，只切除已审阅接口孔和 04 排风口；两面不承担 PCB 贴边定位。原壳实测 04–06 内距约 `103.0 mm`、侧壁厚 `1.8 mm`。当前 X 向暂按 04/06 接口最大突出量分配为 `2.0/1.0 mm`；05/07 继续采用各 `0.20 mm` 紧凑试装余量。原壳 05–07 约 `110.5 mm` 的内距包含不需要复制的额外留空，不得进入当前外壳尺寸。接口突出部分通过贯通孔或在孔内形成退入式接口。

## 6. 已确认的外壳约束

以下约束留给外壳阶段使用，不得在主板参考中提前改写：

- 上壳是顶面、06、05 三个互相连接的面；下壳是底面、04、07 三个互相连接的面。
- 主板先放入上壳，再安装下壳；使用主板原有四孔和对应螺柱定位/夹紧两壳，不另设四个外壳安装螺丝位。
- 四孔夹紧不得使用围绕 `Ø2.5 mm` 导孔的 `Ø3.0 mm` 打印定位鼻（径向壁仅 0.25 mm）；上柱使用平端承托面，装配时由两颗对角紧固件的杆部穿过原 PCB 孔临时对中。
- 顶板、底板厚度精确为 `1.2 mm`。顶面有对准风扇的圆形进气孔，不为风扇上方额外增加间隙或“避让厚度”。
- 外壳 XY 四角使用真实参数化圆角，不得只在预览图上修饰。当前扩展内腔采用 provisional R4；在实测侧壁 `1.8 mm` 下，外圆角必须由内腔等壁厚偏置得到 `R5.8`。禁止恢复已证明会让 PCB 穿出外轮廓的“外 R10 + 小幅外扩”组合。
- 不得让上下壳接缝切开外壳圆角曲面。06/07 圆角完整归上壳，04/05 圆角完整归下壳；两条跨壳竖缝位于圆弧结束后的直边切点。
- PCB 主要由原有四孔和螺柱定位；四周侧壁不贴合定位 PCB。04/06 的 `2.0/1.0 mm` 是 103.0 mm 内跨距和接口投影约束下的 provisional 分配，05/07 各 `0.20 mm` 是紧凑试装余量。PCB R4 未经实测前不得标成 confirmed；额外打印补偿仍须使用独立参数并重新审阅。
- 圆角验证必须同时检查 PCB 被外轮廓完整包含、外轮廓等于 PCB 的等壁厚偏置，以及圆角最小壁厚；仅检查 PCB 与壳体相交体积为零不构成通过依据。
- 复用原机圆形按钮和 `Ø9.4 mm` 面板孔；不要擅自改成大号按钮或方形面板孔。
- 不使用旧版连续卡槽、连续内搭边或绕壳舌槽；若需要新的局部定位结构，必须先给出装配路径和缝隙依据并经过审阅。
- 05、07 没有侧向插拔接口时保持封闭；04 鳍片出风路径不得被封闭横梁阻挡。
- V2 顶板/底板内侧加强筋已经审阅并进入 CAD：筋宽 `1.5 mm`、筋高 `0.8 mm`；基础顶板/底板仍精确为 `1.2 mm`，仅筋路位置局部总厚度为 `2.0 mm`。顶筋必须在 XY 平面绕过完整风扇蜗壳和鳍片包络；底筋必须绕过最低的 04 堆叠双 USB，并同时对完整主板实体执行碰撞检查。任何筋路、宽度或高度变更都必须先重建平面/碰撞审图，再重新导出并回读验证 STEP/STL；不得把局部筋厚写成整板厚度。

## 7. 文件与复现

- 参数和几何源代码：`src/`
- 可执行生成脚本：`scripts/`
- 原始照片：`pics/`
- 审阅 PNG：`previews/`
- STEP/STL 与验证 JSON：`exports/`
- 方法与审阅记录：`docs/`

优先通过 `.venv/bin/python` 运行脚本。修改生成逻辑后必须重新生成产物、执行 `git diff --check`，并确认没有遗留 `__pycache__`。

## 8. 可复用提示词

处理新的照片或新表面时，先执行以下提示：

> 先区分实测尺寸、PCB 对齐坐标、照片轮廓证据和视觉推断。01/02 只确认 PCB 的 100.0 × 105.5 mm 平面，不把总读数平分成接口突出量；接口突出必须逐面、逐件记录。照片只取形，不取全局机械位置。以统一 PCB 坐标生成正投影审图，显示 PCB 边界、毫米比例、中心坐标、尺寸、方向和来源。外壳 04/06 保持带贯通开孔的连续平面墙，并使用独立 PCB 间隙参数，不要求贴边定位。顶/底板基础厚度与局部加强筋必须分开建参；筋路先绕过完整器件包络并对完整主板实体做碰撞检查。未经审阅不要生成对应 STEP/STL；审阅通过后让 CAD 直接读取毫米参数，并验证包络、接触、覆盖、装配关系、加强筋融合状态和导出文件回读一致性。
